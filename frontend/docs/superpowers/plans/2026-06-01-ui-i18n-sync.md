# UI and i18n Synchronization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sync UI atoms (skeletons), enforce 430px layout, and unify i18n headers across templates.

**Architecture:** Use `MainContainer` for layout consistency, `next-intl` for translations, and updated Atomic components for skeletons.

**Tech Stack:** React, Next.js, Tailwind CSS, shadcn/ui, next-intl.

---

### Task 1: Update i18n Messages
**Files:**
- Modify: `src/messages/en.json`
- Modify: `src/messages/vi.json`

- [ ] **Step 1: Update en.json**
Add missing keys to `Music` namespace.
```json
    "your_albums": "Your Albums",
    "add_music": "Add Music",
    "collection_count": "Collection: {count} songs",
    "from_youtube": "From YouTube",
    "recent": "Recent",
    "create": "Create",
    "create_new_album": "Create New Album",
    "album_title": "Album Title",
    "artist_optional": "Artist (Optional)",
    "no_albums_yet": "No albums yet.",
    "create_first_album": "Create your first album",
    "songs": "songs"
```

- [ ] **Step 2: Update vi.json**
Add missing keys to `Music` namespace.
```json
    "your_albums": "Album của bạn",
    "add_music": "Thêm nhạc",
    "collection_count": "Bộ sưu tập: {count} bài hát",
    "from_youtube": "Từ YouTube",
    "recent": "Gần đây",
    "create": "Tạo",
    "create_new_album": "Tạo album mới",
    "album_title": "Tiêu đề Album",
    "artist_optional": "Nghệ sĩ (Tùy chọn)",
    "no_albums_yet": "Chưa có album nào.",
    "create_first_album": "Tạo album đầu tiên",
    "songs": "bài hát"
```

### Task 2: Refine UI Skeletons
**Files:**
- Modify: `src/components/atoms/AlbumSkeleton.tsx`
- Modify: `src/components/atoms/TrackSkeleton.tsx`

- [ ] **Step 1: Update AlbumSkeleton.tsx**
Use `rounded-xl` and ensure it matches the 2-column grid density.
```tsx
export function AlbumSkeleton() {
  return (
    <div className="flex flex-col gap-2">
      <Skeleton className="aspect-square w-full rounded-xl" />
      <div className="space-y-1.5">
        <Skeleton className="h-4 w-3/4 rounded" />
        <Skeleton className="h-3 w-1/2 rounded" />
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Update TrackSkeleton.tsx**
Use `rounded-xl` and match track item layout.
```tsx
export function TrackSkeleton() {
  return (
    <div className="flex items-center gap-4 py-2">
      <Skeleton className="h-12 w-12 rounded-xl shrink-0" />
      <div className="flex-1 space-y-2">
        <Skeleton className="h-4 w-1/3 rounded" />
        <Skeleton className="h-3 w-1/4 rounded" />
      </div>
      <Skeleton className="h-4 w-12 rounded" />
    </div>
  )
}
```

### Task 3: Sync Templates (Headers and Layout)
**Files:**
- Modify: `src/components/templates/HomePageTemplate.tsx`
- Modify: `src/components/templates/AlbumsTemplate.tsx`
- Modify: `src/components/templates/MusicTemplate.tsx`
- Modify: `src/components/templates/AlbumDetailClient.tsx`

- [ ] **Step 1: Update HomePageTemplate.tsx**
Use `MainContainer`, `useTranslations`, and `AlbumSkeleton`.
- [ ] **Step 2: Update AlbumsTemplate.tsx**
Use `MainContainer`, `useTranslations`, and `AlbumSkeleton`.
- [ ] **Step 3: Update MusicTemplate.tsx**
Use `MainContainer` and `useTranslations`.
- [ ] **Step 4: Update AlbumDetailClient.tsx**
Use `MainContainer` and `useTranslations`.

---
**Plan complete and saved to `docs/superpowers/plans/2026-06-01-ui-i18n-sync.md`.**
Two execution options:
1. Subagent-Driven (recommended)
2. Inline Execution
