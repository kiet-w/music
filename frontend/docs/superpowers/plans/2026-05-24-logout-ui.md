# Add Logout UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a logout button to both `BottomTabBar` and `Navbar` components. The action will clear the session and redirect to the login page.

**Architecture:** Utilize `useAuthStore` for session management and `next/navigation`'s `useRouter` for redirection.

**Tech Stack:** React, Zustand (useAuthStore), Next.js Navigation, Lucide React icons.

---

### Task 1: Modify BottomTabBar.tsx

**Files:**
- Modify: `frontend/src/components/molecules/Navigation/BottomTabBar.tsx`

- [ ] Import `LogOut` icon from `lucide-react`.
- [ ] Import `useRouter` from `next/navigation`.
- [ ] Import `useAuthStore` from `@/store/useAuthStore`.
- [ ] Add logout logic: call `clearSession()` and redirect to `/${locale}/login`.
- [ ] Add the logout button to the UI, styled consistently with other tabs.
- [ ] (Optional) Show user's name or email if it fits.

---

### Task 2: Modify Navbar.tsx

**Files:**
- Modify: `frontend/src/components/molecules/Navbar/Navbar.tsx`

- [ ] Import `LogOut` icon from `lucide-react`.
- [ ] Import `useRouter` from `next/navigation`.
- [ ] Import `useAuthStore` from `@/store/useAuthStore`.
- [ ] Import `useLocale` from `next-intl` (it's missing in current `Navbar.tsx`).
- [ ] Add logout logic: call `clearSession()` and redirect to `/${locale}/login`.
- [ ] Add the logout button to the UI.

---

### Task 3: Verification

**Files:**
- N/A

- [ ] Verify that clicking the logout button in `BottomTabBar` clears the session and redirects.
- [ ] Verify that clicking the logout button in `Navbar` clears the session and redirects.
- [ ] Check UI responsiveness and appearance.
