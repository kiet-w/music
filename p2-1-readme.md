# P2-1: AdminController Audit Log
Added `CurrentUser` to extract user details in `deleteTrack` and `cleanupStorage` methods.
Added `Logger.warn` to trace administrative deletions and cleanups in production.
